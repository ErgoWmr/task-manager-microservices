apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  labels:
    app: tasks-service
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: tasks-service
  type: ClusterIP
